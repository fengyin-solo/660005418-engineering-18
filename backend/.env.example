# 日志接口（FastAPI）监听配置
#   HOST: 绑定地址，本地联调默认 127.0.0.1；需要局域网访问可改为 0.0.0.0
#   PORT: 日志接口端口，前端 Vite 代理目标需与此保持一致
LOG_API_HOST=127.0.0.1
LOG_API_PORT=8000

# 允许跨域来源（逗号分隔），对应前端 dev server 地址。
# 修改 VITE_DEV_PORT 后，需把新的前端地址一并加入。
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
